# this version specific adaptation contains adaptations for debian 11 only. The common adaptations are still put in debian
# 1. not exist/valid packages, mark it empty to avoid installing error.
# 2. exist/valid packages, mark it empty if you don't want to install later.
# 3. exist/valid packages, but its name needs to be adapted for other distro,
#    mapping to a new package name, then the new package will be install later.

# reason(1)
winpdb:

# reason(3)
fsverity: fsverity
lib32gcc-dev: lib32gcc-10-dev
libx32gcc-dev: libx32gcc-10-dev
lib64gcc-dev: lib64gcc-10-dev
libx32gcc1: libx32gcc-s1
btrfs-tools: btrfs-progs
libreadline5: readline-common
libntfs: libntfs-3g883
libtirpc: libtirpc-common
libicu: libicu67
libjson: libjson-c5
python3-crypto: python3-pycryptodome
libevent-2.1-6: libevent-2.1-7
libasan: libasan6
