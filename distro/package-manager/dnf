#!/bin/bash

ospkg_update()
{
	support_repo_epel
}

ospkg_install()
{
	local pkg nopkgs
	for pkg
	do
		nopkgs=$(rpm --query $pkg | awk '{print $2}')
		[[ -n $nopkgs ]] &&
		$SUDO yum install -y -q $nopkgs
	done
}
